//const data = Number(require('fs').readFileSync(0, 'utf8'));
/*for (let i = 1; i <= data; i++) { 
    console.log(`3 x ${i} = ${3 * i}`);
}*/

for (let i = 1; i <= 3; i++) { 
    console.log(`3 x ${i} = ${3 * i}`);
}